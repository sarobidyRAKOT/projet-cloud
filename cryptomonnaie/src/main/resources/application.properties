spring.application.name=cryptomonnaie

spring.datasource.url=jdbc:postgresql://localhost:5432/gmail
spring.datasource.username=postgres
spring.datasource.password=Andyh123.

# Dialecte JPA pour PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Afficher les requêtes SQL générées
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate - créer les tables à partir des entités
spring.jpa.hibernate.ddl-auto=update
